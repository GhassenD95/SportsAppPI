<form method="post" action="{{ path('app_team_performance_delete', {'id': team_performance.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ team_performance.id) }}">
    <button class="btn">Delete</button>
</form>
