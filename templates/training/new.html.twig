{% extends 'base.html.twig' %}

{% block title %}New Training{% endblock %}

{% block body %}
<div class="container mx-auto px-4 py-8">
    <div class="max-w-3xl mx-auto bg-white p-8 rounded-lg shadow-xl">
        <h1 class="text-3xl font-bold text-gray-800 mb-8">Create New Training Session</h1>

        {{ include('training/_form.html.twig', {
            'button_label': 'Create Training',
            'button_class': 'w-full bg-brand-blue hover:bg-blue-700 text-white font-bold py-3 px-4 rounded-lg focus:outline-none focus:shadow-outline transition duration-150 ease-in-out'
        }) }}

        <div class="mt-8 text-center">
            <a href="{{ path('app_training_index') }}" class="text-brand-blue hover:text-blue-700 font-medium transition duration-150 ease-in-out">
                <i data-feather="arrow-left" class="inline-block h-4 w-4 mr-1"></i>Back to List
            </a>
        </div>
    </div>
</div>
{% endblock %}
