<form method="post" action="{{ path('app_training_exercise_delete', {'id': training_exercise.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ training_exercise.id) }}">
    <button class="btn">Delete</button>
</form>
