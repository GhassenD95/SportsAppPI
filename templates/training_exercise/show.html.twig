{% extends 'base.html.twig' %}

{% block title %}TrainingExercise{% endblock %}

{% block body %}
    <h1>TrainingExercise</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ training_exercise.id }}</td>
            </tr>
            <tr>
                <th>DurationMinutes</th>
                <td>{{ training_exercise.durationMinutes }}</td>
            </tr>
            <tr>
                <th>Intensity</th>
                <td>{{ training_exercise.intensity }}</td>
            </tr>
            <tr>
                <th>Notes</th>
                <td>{{ training_exercise.notes }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_training_exercise_index') }}">back to list</a>

    <a href="{{ path('app_training_exercise_edit', {'id': training_exercise.id}) }}">edit</a>

    {{ include('training_exercise/_delete_form.html.twig') }}
{% endblock %}
